# 参与砚白

这份文件说明怎么反馈问题、怎么提交改动，以及提交改动需要先同意什么。软件的使用条款在 `LICENSE.md` 与 `LICENSE_zh.md`，本文件不改变那些条款。

## 反馈问题与提问

不需要下载或安装软件就能提。入口按内容选：

- **公开渠道**：[Yanbai-Releases 的 Issues](https://github.com/Speechlessmanbilibili/Yanbai-Releases/issues)。一般缺陷、功能建议、用法疑问都走这里。
- **电子邮件**：<speechlessmans@outlook.com>。含机密信息、个人信息，或不便公开的内容走这里。Issue 是公开且可检索的，请不要把这类内容贴进 Issue。

报告缺陷时，把这几项写清定位会快很多：

1. 版本号（安装目录里 `Yanbai.exe` 的文件版本，或压缩包文件名）；
2. 系统版本与架构（Windows 10/11、x64 还是 ARM64；Linux 的发行版与内核）；
3. 复现步骤，越具体越好；
4. 期望的结果与实际结果；
5. 能公开的最小样例文档。**不要附含机密或个人信息的文档**，需要时请自行脱敏或另造一份样例。

## 提交改动

源码不公开，仓库不接受外部 Pull Request。想提交改动请走邮件：

1. 先用邮件说明你想改什么、为什么改，确认方向；
2. 同意 `CLA.md` 里的贡献者许可协议，在邮件里写明接受，并给出署名与联系方式；
3. 用 `git format-patch` 生成补丁，或附上统一 diff，随邮件发来。

没有同意 CLA 的补丁不会被合并，这条没有例外。

## 代码标准

改动要满足仓库现有的验收口径：

- `cargo test --workspace` 全过；
- `cargo clippy --workspace --all-targets -- -D warnings` 干净；
- 前端改动跑 `pnpm --prefix frontend test` 与 `pnpm --prefix frontend lint`；
- 新增或修改解析分支时同步补用例，重点是截断、畸形字节与空输入；
- 提交信息用中文简述改了什么，一次提交只做一件事，不要加任何 AI 署名或免责声明；
- 不要为了迎合工具而删掉错误处理、边界检查或把断言写弱。

## 语言

反馈用中文或英文都可以。补丁里的注释与提交信息用中文，与仓库现状保持一致。
